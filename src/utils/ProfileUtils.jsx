
export const CreateNewProfile = ({id,role,name,birth_day,avatarURI})=>{
    return {
        id:id,
        role:role,
        name:name,
        birth_day:birth_day,
        avatarURI:avatarURI,
        password,
    }
}